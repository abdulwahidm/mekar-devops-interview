#!/bin/bash

# print a message that the script has started executing
echo "Starting automation script."

# create a directory named "new_folder"
mkdir new_folder

# change into the "new_folder" directory
cd new_folder

# create a file named "example.txt" and write "Hello, world!" into it
echo "Hello, world!" > example.txt

# see all the files in the "new_folder" directory
ls

